# Theo 아키텍처 원칙

2026-08-13. 이 문서는 "Theo를 왜 이런 모양으로 짓는가"를 적어둔 것이다.
원칙들은 이론에서 온 게 아니라, 첫 파일럿(남편)의 실제 사고들에서
도출됐다. 코드를 고치거나 새 기능을 붙일 때, 이 문서와 어긋나는
방향이면 멈추고 다시 생각한다.

## 제품이 뭔가 (한 문장)

Theo는 유저의 companion이다. 대화하면서 유저에게 지금 필요한 게
뭔지 알아내고, 검증된 학습·지식 관리 방법들을 유저가 눈치채지 못하게
대화 속에서 실행해준다. (남편은 "spaced repetition"이라는 말을 들어본
적 없이 아침 문제를 받는다 — 방법론을 가르치는 게 아니라 Theo가
방법론 그 자체로 행동한다.)

## 트랙과 "두 종류"

유저 한 명 아래에는 **트랙**(진행 중인 일 하나)이 계속 늘어난다.
남편이라면: PDF 드릴(지금) → BD companion(요청함) → 바 시험(내년)
→ 그 뒤에 뭐가 올지는 모른다(주니어 교육? 첫 매니징?). 트랙 수에는
제한이 없다.

아래의 "두 종류"는 트랙의 개수가 아니라, **새 트랙이 열릴 때마다
던지는 첫 질문**이다: "이 일은 답을 채점할 수 있는 일인가?"
그 답에 따라 필요한 기계가 갈린다 — 그리고 트랙이 5개가 돼도
기계는 몇 개 안 늘어난다는 게 이 구조의 핵심이다. (예: 바 시험은
새 트랙이지만 새 기계가 아니다 — 지금의 드릴 모드를 재사용한다.)

1. **채점이 있는 일** (예: 드릴 — 답이 맞고 틀리고가 있다).
   이런 일은 전용 기계 한 벌이 필요하다. 이 기계 한 벌을 **모드**라고
   부른다. 지금 모드는 드릴 하나다.
2. **대화만으로 되는 일** — 답을 채점할 수 없는, 동행이 본체인 일.
   companion 우산(BD companion, founder companion, 사이드 허슬
   companion)이 전부 여기다. 예: BD 스레드 챙기기("그 MD 연락한 지
   석 달 됐다", 미팅 후 수확), 생각 정리 상대(아이디어를 던지면
   받아서 되물어주는 것), 임장 갈 곳 리스트 뽑기, 시키지 않은 일에
   대한 책임감 동반. 기반 모델의 대화 능력 +
   공용 장치(기억, 먼저 연락, 거짓말 방지)로 실행된다.
   단, companion의 뼈대(스레드를 장부에 담고 리듬을 지키는 것)는
   한 번은 지어야 한다 — 지인 파일럿 때 짓는다. "비용이 거의 0"인
   것은 그 뼈대가 선 다음의 활동 추가다: 임장 리스트든 책 쓰기
   동행이든, 새 기계 없이 대화가 곧 실행이다.

무덤을 파는 길은 2번까지 1번 수준의 엄밀함을 약속하는 것이다.
성립하는 길은: 1번 기계는 천천히 하나씩 늘리고(유저가 당길 때만),
2번은 기반 모델에 맡기되 공용 장치로 신뢰를 지키는 것.

---

## 원칙 1. LLM 호출 하나에는 일 하나만 시킨다

이번 주의 품질 사고 전부가 "한 호출이 여러 일을 하던 곳"에서 났다:

| 사고 | 한 호출이 하던 일들 | 수리 |
|---|---|---|
| 초기의 이상한 문제들 (남편: "사람이면 안 할 이해") | 주제 고르기 + 내용 기억해내기 + 문제 쓰기 + 대화하기 | 드릴 엔진: 채굴/선정/예측/표현/채점을 각각 다른 호출로 (#156) |
| 문서의 서로 다른 부분을 한 문제에 섞음 | 50페이지 통째로 읽기 + 아이템 만들기 | 섹션 단위 채굴로 재설계 예정 |
| 수요일 정오의 엉터리 후속 문제 | 답 피드백 + 다음 문제 창작을 답장 한 호출이 | 후속 문제도 은행 경로로 (#173) |

여러 단계(우리끼리 말로 **홉**)로 나누는 이유는 두 가지다:
홉마다 일이 단순해져서 그 홉의 정확도가 오르고, 홉과 홉 사이에
코드 검사를 끼울 자리가 생긴다. 한 번에 다 시키면 검문소를
세울 곳이 없다.

## 원칙 2. 홉 사이는 코드로 검사한다

LLM에게 "잘 확인해"라고 시키는 건 검사가 아니다. 검사는 코드가 한다:

- 문제 은행 아이템은 **문서에 글자 그대로 존재하는 인용**(anchor)이
  없으면 저장 자체가 거부된다 (substring 검사).
- 예측은 한 번 쓰면 **수정 불가**, 채점도 딱 한 번 (write-once).
  고칠 수 있는 예측은 KPI를 거짓말로 만든다.
- 유저 선호는 유저의 **실제 발화가 글자 그대로** 대화에 있는지
  검증하고 저장한다 (증거 없는 선호는 버린다).
- 발송 직전에 답 유출, 수령 조작("파일 읽어봤어") 등을 정규식으로
  잡아서 재작성시킨다.

왜 코드여야 하나: LLM 판단만 있는 실패는 **티가 안 난다** — 시스템이
성공한 척 결과를 내놓고, 유저가 지적해야 비로소 드러난다. 코드 검사는
실패를 이벤트로 남긴다.

주의: 홉을 나누기만 하고 검사를 안 끼우면 오히려 나빠진다.
홉마다 90% 정확하면 세 홉이면 73%다. 나누기 + 검사가 한 세트다.

## 원칙 3. 거짓말만은 구조적으로 불가능하게 만든다

남편이 가르쳐준 것: 유저는 **실력의 불완벽**은 용서한다 (틀린 걸
자기가 고쳐주는 걸 오히려 즐겼다). 하지만 **성실성의 불완벽** —
지어내기, 받은 적 없는 파일을 "읽어봤다"고 하기, "네 파일에 있다"고
뒤집어씌우기 — 에는 매번 신뢰가 깎였다.

그래서 지키는 완벽은 하나다: Theo는 자기가 뭘 알고 뭘 했는지에
대해 거짓말할 수 없어야 한다. 위 원칙 2의 장치들이 이걸 위해 있다.
기능이 부족한 건 괜찮다. 모르는 건 모른다고 말하면 된다.

## 원칙 4. 장부가 이음새다

사업의 실체는 잘 만든 프랑켄슈타인이다 — 부품이 늘어나는 건 문제가
아니고, 부품들이 제멋대로 붙는 게 문제다. Theo에서 모든 부품을
연결하는 뼈대는 **장부(ledger)**다:

- 문제 은행 (앵커 달린 아이템들)
- 오답 노트 (요소별 채점 기록)
- 예측 장부 (문제 내기 전에 적는 "그가 맞힐까" — 적중률이 KPI)
- 가르쳐준 것 장부 (유저의 정정 — 진실 서열: 유저 정정 > 문서 > 모델)
- 사람 노트 ("이 사람은 이럴 때 이렇게 한다" + 발화 증거)
- 유저 선호 (상시 규칙 — 언어, 출제 범위, 문제 스타일)

새 부품(모드, 기능)은 반드시 이 장부들을 읽고 쓰게 만든다.
그래야 드릴에서 배운 것("모호한 설정을 주면 명료화부터 요구한다")이
나중에 BD companion에서도 쓰이고, 부품이 20개가 돼도 유저 입장에선
"나를 아는 하나의 존재"로 느껴진다. 감동은 기능 개수가 아니라
여기서 나온다.

## 원칙 5. 모드는 라이브러리처럼, 유저가 당길 때만 추가한다

모드(전용 기계 한 벌)는 비싸다. 유저·도메인·자료마다 새 모드가
필요한 게 아니라, **문제의 종류**가 다를 때만 새 모드가 필요하다:

- 산문 답을 채점하는 드릴 (지금 있음) — 변호사든 세무사든 같은 기계
- 조합형 시나리오 문제 (의대 감별진단, 로스쿨 issue-spotting) — 필요해지면
- 풀이 과정 검증, 산출물(글/코드) 리뷰 — 필요해지면

새 모드는 유저가 실제로 원할 때만 짓는다 (남편이 Theo에게 직접
말해야 트랙이 열린다는 규칙과 같은 원리). 미리 짓지 않는다.

## 원칙 6. 유저가 디버거다

오퍼레이터가 모든 대화를 읽는 방식은 유저 5명에서 죽는다. 확장되는
방식은 유저의 지적이 자동으로 시스템을 고치는 것이다:

- 유저가 코치의 틀린 말을 정정하면 → 가르쳐준 것 장부에 들어가서
  이후 모든 문제와 피드백을 구속한다.
- 유저가 "이 문제 이상한데"라고 하면 → 그 아이템은 정지되고,
  컴플레인이 기록되고, 채굴 기준이 배운다. (재설계에서 복귀 예정)
- 모든 수리에는 꼬리표를 단다: [플랫폼] / [모드] / [콘텐츠 루프] /
  [유저 전용]. 파일럿 5명째에 신규 유저당 엔지니어링이 급감하지
  않으면 — 이 아키텍처의 전제가 틀린 것이니 그때 좁힌다.

## 하지 않는 것

- **자율 에이전트 루프**: LLM이 스스로 계획하고 도구를 고르고 알아서
  끝내는 방식. 이번 주 사고 일지가 반증했다. 경로는 코드가 소유하고,
  LLM은 지정된 홉에서 판단만 한다.
- **미리 짓기**: 관측성 대시보드는 파일럿 코호트 직전에, 진단 엔진은
  companion 루프 다음에, vignette 기계는 그 유저가 나타나면.
- **모든 홉의 무오류 추구**: 도달 불가능하다. 대신 모든 홉이 기록을
  남기고, 오류가 유저의 문자 한 통으로 싸게 잡히게 만든다.

---

## 현재 파이프라인 지도 (2026-08-13 기준)

**아침 발송** (스케줄 크론):
```
서버 게이트 (일시정지? 스킵? 시간대?)          ← 코드
→ 선정: 은행에서 오늘의 문제 고르기            ← LLM 판단 (rank_select)
→ 예측: "그가 맞힐까" 기록                     ← LLM 판단, write-once 저장
→ 표현: 아이템을 문자로 (앵커·루브릭 동봉)     ← LLM 작문
→ 발송 가드 (답 유출? 질문 2개? 조작?)         ← 코드, 위반 시 재작성
→ 발송 + 이벤트 기록
```

**유저 답장** (인바운드):
```
선호·상태 추출 (analyze)                       ← LLM 판단 + 증거 검증(코드)
→ 채점: 답 하나에서 신호 4개                   ← LLM 판단
   (요소별 채점 / 자기확신 / 스타일 관찰 / 예측 채점)
→ 다음 문제 준비 (은행 경로, 예측 먼저)         ← #173
→ 답장 작성 (채점 결과 + 앵커 + 다음 문제)      ← LLM 작문
→ 발송 가드 → 발송 + 기록
```

**은행 보충** (자동): 미출제 문제가 5개 아래로 떨어지면, 자료 + 유저
선호(출제 범위·스타일) + 오답 기록을 넣고 새 문제를 채굴한다.
채굴 방식은 섹션 단위로 재설계 예정 (#172 리버트의 후계).
