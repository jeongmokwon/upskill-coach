# Theo 아키텍처 v1.0

2026-08-13. 이 문서는 Theo가 실제로 어떻게 도는지의 지도다.
시스템이 바뀔 때마다 이 문서도 같이 갱신한다.

## 제품 (한 문장)

Theo는 유저의 career companion이다. 유저와 대화하면서 유저
커리어에서 지금 필요한 게 뭔지 알아내어 도와준다.

## 트랙, 그리고 일의 종류

유저 한 명 아래에는 **트랙**(진행 중인 일 하나)이 계속 늘어난다.
남편이라면: PDF 드릴(지금) → BD companion(요청함) → 바 시험(내년)
→ 그 뒤에 뭐가 올지는 모른다. 트랙 수에 제한이 없고, 트랙이
요구하는 **일의 종류도 미리 셀 수 없다 — n종류다.**

지금까지 발견된 종류 (완결된 목록이 아니다):

- **대화만으로 되는 일** — 생각 정리 상대, 조언, 리스트 뽑기.
- **리마인더** — 스레드, 약속, 목표/KPI 등.
- **채점이 있는 일** — 드릴처럼 답이 맞고 틀리는 일. 전용 검증
  기계 한 벌(**모드**)이 필요하다. 지금 모드는 드릴 하나.

아직 안 만났지만 올 것: 완전히 새로운 지식을 습득하는 깊은 학습,
유저의 파일·화면을 같이 보며 일하는 것, 그 외 상상 못 한 것들.
**새로운 트랙 생성이 필요하다고 판단되면, founder에게 알린다.
founder에게 notify 하는 매커니즘은 pilot 단계에서 추가가 필요하다.
새 종류의 발견은 사고가 아니라 정상 이벤트다.**

### 새 부품이 필요한 요구가 나오면 (제품의 작동 방식)

유저와 대화하다가, 대화의 결과가 **새로운 부품을 요구하는 작업**이면
유저에게 "이건 우리가 만들어 가지고 와야 한다"라고 말한다. 그리고
며칠에 걸쳐 사람과 LLM이 같이 작업해서 만들어 deliver한다. 그동안
대화·리마인더 층은 계속 돌아간다 — 유저 경험은 멈추지 않는다.
기존 부품의 조립만으로 되는 경우에도, 당분간은 막판 검수를 거친다.

이건 임시방편이 아니라 제품의 성격이다: 즉석에서 흉내내다 티 안
나게 실패하는 것보다, "만들어서 가져오겠다"가 정직하고, 장기 career
companion이라면 며칠은 문제가 되지 않는다.

---

## 장부 지도

모든 부품이 읽고 쓰는 공유 상태. 어떤 부품이 쓰고 어떤 부품이
읽는지가 시스템의 실제 배선이다.

| 장부 | 쓰는 곳 | 읽는 곳 |
|---|---|---|
| 문제 은행 (knowledge_items) | 임포트, 채굴 모듈; 상태 변경은 채점·운영자 | 선정, 출제, 채점 |
| 오답 노트 (attempts) | 채점 패스 (요소별 판정) | 선정, 예측, 채굴 |
| 예측 장부 (predictions) | 발송 직전 예측 (write-once); 채점이 결과 기입 (1회만) | KPI(적중률), "열린 질문" 상태 |
| 가르쳐준 것 (taught_ledger) | 채점 패스 (유저가 코치를 정정할 때) | **아직 없음 — 연결 예정** (채굴·출제가 읽어야 함) |
| 사람 노트 (person_notes) | 채점 패스 (증거 있는 관찰), 임포트 | 선정, 예측, 채굴, 출제 |
| 유저 선호 (user_preferences) | analyze (verbatim 증거 검증 후) | 모든 프롬프트의 선호 블록, 채굴, 선정 |
| 기타 상태 | phase, 스케줄, pause, 자료(원문+다이제스트), 이벤트 로그 | 전 파이프라인 |

---

## 파이프라인 지도

**트랙 하나 = 파이프라인 하나.** "드릴"은 파이프라인이 아니라
가족 이름이고, 같은 드릴 가족 안에도 파이프라인이 여러 개일 수
있다: 남편 PDF 드릴(산문 회상), 바 시험 드릴(객관식 세트 + 시험일
역산 커버리지 + 정답표 대조 채점), 의대식 vignette 드릴 — 전부
"드릴"이라 불리지만 서로 다른 기계다. 아래에 그린 파이프라인들은
그중 하나, **드릴 v1 — 남편형(대화식 산문 회상)**이다.

(코드 메모: tracks.mode 컬럼이 지금 'drill'이라는 가족 이름을
저장하고 있다. 두 번째 파이프라인이 생기는 날 이 값은 구체
파이프라인 식별자('drill_prose_v1' 등)로 세분화되어야 한다.)

파이프라인이 없는 유저(예: founder, 지금의 jeongmo)의 발송은
아래를 전부 건너뛰고 대화형 경로로 간다.

모드끼리 공유하는 것은 기반뿐이다: 장부, 발송(버블 순서·가드),
이벤트 기록, 스케줄, 그리고 유저에 대한 지식(사람 노트·선호).

### 1. 스케줄 발송 — 드릴 v1 (남편형)

```
서버 게이트                                    [코드]
  오늘 스킵? / 일시정지? / 스케줄 창?
→ 선정 (rank_select)                           [LLM 판단]
  입력: 출제 가능 아이템 + 오답 기록 + 사람 노트 + 선호
  출력: 오늘의 아이템 + p_miss + 이유
  실패 시: 결정론적 점수 계산으로 폴백
→ 예측 (_predict)                              [LLM 판단 → write-once 저장]
  "그가 이 문제를 맞힐까" — 문제가 나가기 전에 기록됨
→ 출제문 작성                                  [LLM 작문]
  입력: 아이템(주제+루브릭+앵커) + 사람 노트 + 선호 + 직전 채점의 앵커
→ 발송 가드                                    [코드, 위반 시 재작성]
  질문 2개? / 수령 조작("파일 읽어봤어")? / 답 유출(앵커·루브릭 노출)?
→ 버블 발송 (순서 게이트: 첫 버블 인계 확인 후 둘째)
→ 기록: messages, cron_tick, sms_out, drill_question_sent, llm_calls
```

### 2. 유저 답장 — 드릴 v1 (인바운드)

```
analyze                                        [LLM 판단 + 코드 검증]
  전체 대화 재독 → 온보딩 필드·선호·pause 추출
  선호는 verbatim 증거가 실제 대화에 있어야만 저장
→ 채점 (grade_if_answering)                    [LLM 판단]
  열린 예측이 있고 이 답장이 그 답이면:
  답 하나 → 신호 4개:
    요소별 채점        → 오답 노트
    전체 판정          → 아이템 상태 이동 (solid/learning)
    자기확신 + 헤지 표현 → 오답 노트
    예측 채점          → KPI
  (+ 조건부: 스타일 관찰 → 사람 노트 / 코치 정정 → 가르쳐준 것)
  ※ "이 문제 이상해" 컴플레인 처리는 #172 롤백으로 부재 — 재설계에 포함 예정
→ 다음 문제 준비                               [#173: 아침 발송과 같은 선정+예측]
  채점이 일어났을 때만. 은행에 문제가 없으면 질문 없음.
→ 답장 작성                                    [LLM 작문]
  입력: 채점 결과 + 그 아이템의 앵커("앵커 밖은 단정 금지")
       + 다음 아이템 블록 (있으면)
       + 드릴 유저인데 서빙된 아이템이 없으면: 프리랜스 질문 금지 블록
→ 발송 가드 (아침 발송과 동일 + 후속 문제 답 유출 검사)
→ 발송 → 기록 (sms_out, drill_question_sent{followup})
```

### 3. 은행 보충 — 드릴 v1 (자동)

```
채점 후 미출제 잔고 확인                        [코드]
  5개 미만이면 백그라운드로:
→ 채굴 (generate_items)                        [LLM]
  입력: 자료 원문 + 유저 선호(출제 범위·스타일) + 오답 기록
       + 사람 노트 + 기존 은행(중복 방지)
→ 앵커 검증                                    [코드]
  인용이 자료에 글자 그대로 없으면 재앵커 1회 → 그래도 없으면 탈락
  ※ 섹션 단위 채굴로 재설계 예정 (#172 롤백의 후계 —
    문서를 통째로 읽다가 서로 다른 부분이 한 문제에 섞이는 사고 방지)
→ 은행에 추가 (untested) + bank_refilled 이벤트
```

### 4. 야간 (Render 크론)

```
annotate: 하루치 대화 주석 (운영자 관찰용)
※ EXPECT 채점 부분은 step 표면 은퇴(#171)로 유휴 상태
```

---

## 예정된 변경 (순서대로)

1. **채굴 chunking 재설계** — 섹션 단위 채굴 + "이 문제 이상해"
   컴플레인 루프 복귀 + 선호→섹션 조준이 기록에 남게
2. **가르쳐준 것 장부 소비자 연결** — 지금 쓰기만 되고 아무도 안
   읽는다 (위 장부 지도 참고)
3. **companion 뼈대** — 스레드·약속 장부 + 리듬 (지인 파일럿 설계와
   함께)
4. **관측성** — 운영자가 transcript를 다 읽는 대신 유저별
   KPI + 이상 이벤트만 보게 (파일럿 코호트 직전)
