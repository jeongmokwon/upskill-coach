# Theo 아키텍처 v1.1

2026-08-13 작성, 2026-08-18 정체성 피봇 반영. 이 문서는 Theo가
실제로 어떻게 도는지의 지도다. 시스템이 바뀔 때마다 이 문서도
같이 갱신한다.

## 제품 (한 문장)

**Theo는 유저의 life companion이다.** 일과 일상을 구분하지 않고,
유저가 매일 머릿속에서 굴리는 것들(멘탈 로드)을 대화로 알아내어
대신 들어준다. (2026-08-18 founder 선언: edtech이 아니다. 커리어는
여러 트랙 중 하나 — 지불 근거로는 가장 크지만, 정체성은 아니다.)

## 트랙, 그리고 일의 종류

유저 한 명 아래에는 **트랙**(진행 중인 관심사 하나)이 계속
늘어난다. 남편이라면: PDF 드릴(지금) → BD companion(요청함) → 바
시험(내년). jeongmo라면: 장보기 리스트, 살림 stock up, 부모님 전화
주기, 스타트업 업무... 트랙 수에 제한이 없고, 트랙이 요구하는
**일의 종류도 미리 셀 수 없다 — n종류다.**

**트랙은 코드가 아니라 설정 데이터다 (#178).** role(한 줄 임무) +
part_type(어느 기계가 돌리나) + surfacing(언제 수면 위로 올릴 수
있나) + donts. 생성·수정·보류·부활·은퇴 전부 대화로 — operator
게이트 없음("알아듣고 알아서 해내는가"가 관찰 포인트다).

부품 레지스트리 v1 (2026-08-18 리허설에서 도출, 완결된 목록 아님):
capture_list(받아적는 리스트) · cadence(주기 추적) ·
owed_ledger(갚을 것 목록) · event_watch(날짜 추적) ·
research(취향 기반 추천) · companion(생각 정리+스레드).

**두 구역의 경계:** 설정·데이터로 표현되는 변화는 대화에서 자유롭게
흐른다(구역 A — 주기 파라미터, donts, 진화하는 아이템 payload,
라이프사이클). 존재하지 않는 기계가 필요한 요구는 절대 끼워맞추지
않는다(구역 B) — new_part_requested 이벤트(founder notify)가 되고,
코치는 "이건 우리가 만들어서 가져와야 한다"고 말한다. part_type은
트랙당 불변: 기계를 바꾸는 건 설정 수정이 아니라 새 부품 결정이다.

지금까지 발견된 종류 (완결된 목록이 아니다):

- **대화만으로 되는 일** — 생각 정리 상대, 조언, 리스트 뽑기.
- **리마인더** — 스레드, 약속, 목표/KPI 등.
- **채점이 있는 일** — 드릴처럼 답이 맞고 틀리는 일. 전용 검증
  기계 한 벌(**모드**)이 필요하다. 지금 모드는 드릴 하나.

아직 안 만났지만 올 것: 완전히 새로운 지식을 습득하는 깊은 학습,
유저의 파일·화면을 같이 보며 일하는 것, 그 외 상상 못 한 것들.
**새 부품이 필요하다고 판단되면 founder에게 알린다 — 매커니즘 v1은
new_part_requested 이벤트(#178, 운영자가 timeline에서 읽음)이고,
push 채널은 나중이다. 새 종류의 발견은 사고가 아니라 정상
이벤트다.**

### 새 부품이 필요한 요구가 나오면 (제품의 작동 방식)

유저와 대화하다가, 대화의 결과가 **새로운 부품을 요구하는 작업**이면
유저에게 "이건 우리가 만들어 가지고 와야 한다"라고 말한다. 그리고
며칠에 걸쳐 사람과 LLM이 같이 작업해서 만들어 deliver한다. 그동안
대화·리마인더 층은 계속 돌아간다 — 유저 경험은 멈추지 않는다.
기존 부품의 조립만으로 되는 경우에도, 당분간은 막판 검수를 거친다.

이건 임시방편이 아니라 제품의 성격이다: 즉석에서 흉내내다 티 안
나게 실패하는 것보다, "만들어서 가져오겠다"가 정직하고, 장기 career
companion이라면 며칠은 문제가 되지 않는다.

---

## 장부 지도

모든 부품이 읽고 쓰는 공유 상태. 어떤 부품이 쓰고 어떤 부품이
읽는지가 시스템의 실제 배선이다.

| 장부 | 쓰는 곳 | 읽는 곳 |
|---|---|---|
| 문제 은행 (knowledge_items) | 임포트, 채굴 모듈; 상태 변경은 채점·운영자 | 선정, 출제, 채점 |
| 오답 노트 (attempts) | 채점 패스 (요소별 판정) | 선정, 예측, 채굴 |
| 예측 장부 (predictions) | 발송 직전 예측 (write-once); 채점이 결과 기입 (1회만) | KPI(적중률), "열린 질문" 상태 |
| 가르쳐준 것 (taught_ledger) | 채점 패스 (유저가 코치를 정정할 때) | **아직 없음 — 연결 예정** (채굴·출제가 읽어야 함) |
| 사람 노트 (person_notes) | 채점 패스 (증거 있는 관찰), 임포트 | 선정, 예측, 채굴, 출제 |
| 유저 선호 (user_preferences) | analyze (verbatim 증거 검증 후) | 모든 프롬프트의 선호 블록, 채굴, 선정 |
| 트랙 설정 (tracks의 life 칼럼들) | tracks_ops 홉 (코드 검증 후) | 답장 프롬프트의 트랙 블록; 지휘 층(예정) |
| 트랙 아이템 (track_items) | tracks_ops 홉 (부품별 최소 계약 검증) | 트랙 블록; 지휘 층(예정) |
| 리마인더 (reminders) | 운영자 시딩 (/debug/reminder — v1에서 대화 마커로 대체 예정) | /reminders/tick (15분 cron) → send_nudge; 발화·실패는 이벤트 로그에 |
| 기타 상태 | phase, 스케줄, pause, 자료(원문+다이제스트), 이벤트 로그 | 전 파이프라인 |

---

## 파이프라인 지도

**트랙 하나 = 파이프라인 하나.** "드릴"은 파이프라인이 아니라
가족 이름이고, 같은 드릴 가족 안에도 파이프라인이 여러 개일 수
있다: 남편 PDF 드릴(산문 회상), 바 시험 드릴(객관식 세트 + 시험일
역산 커버리지 + 정답표 대조 채점), 의대식 vignette 드릴 — 전부
"드릴"이라 불리지만 서로 다른 기계다. 아래에 그린 파이프라인들은
그중 하나, **드릴 v1 — 남편형(대화식 산문 회상)**이다.

(코드 메모: tracks.mode 컬럼이 지금 'drill'이라는 가족 이름을
저장하고 있다. 두 번째 파이프라인이 생기는 날 이 값은 구체
파이프라인 식별자('drill_prose_v1' 등)로 세분화되어야 한다.)

파이프라인이 없는 유저(예: founder, 지금의 jeongmo)의 발송은
아래를 전부 건너뛰고 대화형 경로로 간다.

모드끼리 공유하는 것은 기반뿐이다: 장부, 발송(버블 순서·가드),
이벤트 기록, 스케줄, 그리고 유저에 대한 지식(사람 노트·선호).

### 1. 스케줄 발송 — 드릴 v1 (남편형)

```
서버 게이트                                    [코드]
  오늘 스킵? / 일시정지? / 스케줄 창?
→ 선정 (rank_select)                           [LLM 판단]
  입력: 출제 가능 아이템 + 오답 기록 + 사람 노트 + 선호
  출력: 오늘의 아이템 + p_miss + 이유
  실패 시: 결정론적 점수 계산으로 폴백
→ 예측 (_predict)                              [LLM 판단 → write-once 저장]
  "그가 이 문제를 맞힐까" — 문제가 나가기 전에 기록됨
→ 출제문 작성                                  [LLM 작문]
  입력: 아이템(주제+루브릭+앵커) + 사람 노트 + 선호 + 직전 채점의 앵커
→ 발송 가드                                    [코드, 위반 시 재작성]
  질문 2개? / 수령 조작("파일 읽어봤어")? / 답 유출(앵커·루브릭 노출)?
→ 버블 발송 (순서 게이트: 첫 버블 인계 확인 후 둘째)
→ 기록: messages, cron_tick, sms_out, drill_question_sent, llm_calls
```

### 2. 유저 답장 — 드릴 v1 (인바운드)

```
analyze                                        [LLM 판단 + 코드 검증]
  전체 대화 재독 → 온보딩 필드·선호·pause 추출
  선호는 verbatim 증거가 실제 대화에 있어야만 저장
→ 채점 (grade_if_answering)                    [LLM 판단]
  열린 예측이 있고 이 답장이 그 답이면:
  답 하나 → 신호 4개:
    요소별 채점        → 오답 노트
    전체 판정          → 아이템 상태 이동 (solid/learning)
    자기확신 + 헤지 표현 → 오답 노트
    예측 채점          → KPI
  (+ 조건부: 스타일 관찰 → 사람 노트 / 코치 정정 → 가르쳐준 것)
  ※ "이 문제 이상해" 컴플레인 처리는 #172 롤백으로 부재 — 재설계에 포함 예정
→ 다음 문제 준비                               [#173: 아침 발송과 같은 선정+예측]
  채점이 일어났을 때만. 은행에 문제가 없으면 질문 없음.
→ 답장 작성                                    [LLM 작문]
  입력: 채점 결과 + 그 아이템의 앵커("앵커 밖은 단정 금지")
       + 다음 아이템 블록 (있으면)
       + 드릴 유저인데 서빙된 아이템이 없으면: 프리랜스 질문 금지 블록
→ 발송 가드 (아침 발송과 동일 + 후속 문제 답 유출 검사)
→ 발송 → 기록 (sms_out, drill_question_sent{followup})
```

### 3. 은행 보충 — 드릴 v1 (자동)

```
채점 후 미출제 잔고 확인                        [코드]
  5개 미만이면 백그라운드로:
→ 채굴 (generate_items)                        [LLM]
  입력: 자료 원문 + 유저 선호(출제 범위·스타일) + 오답 기록
       + 사람 노트 + 기존 은행(중복 방지)
→ 앵커 검증                                    [코드]
  인용이 자료에 글자 그대로 없으면 재앵커 1회 → 그래도 없으면 탈락
  ※ 섹션 단위 채굴로 재설계 예정 (#172 롤백의 후계 —
    문서를 통째로 읽다가 서로 다른 부분이 한 문제에 섞이는 사고 방지)
→ 은행에 추가 (untested) + bank_refilled 이벤트
```

### 4. 야간 (Render 크론)

```
annotate: 하루치 대화 주석 (운영자 관찰용)
※ EXPECT 채점 부분은 step 표면 은퇴(#171)로 유휴 상태
```

### 5. 트랙 운영 홉 — life 트랙 (#178)

```
lane 게이트 (user_profiles.tracks_enabled)      [코드]
  닫힌 유저(남편)는 프롬프트·경로 모두 이전과 동일
→ 답장 프롬프트에 트랙 블록                     [프롬프트]
  현재 트랙 상태 + 설계 대화 규칙 (합의 전 "돌아간다" 주장 금지,
  보류도 유효한 결론, 구역 B는 정직하게 "만들어 가져오겠다")
→ 답장 발송 후, 백그라운드로 ops 홉             [LLM 판단]
  대화 → 운영들(create/update/status/아이템) + verbatim 증거
→ 코드 검증                                     [코드, 홉 사이 계약]
  부품 레지스트리 / surfacing 스키마 / 소유권 / 중복명 붕괴
  / part_type 불변; 탈락은 이벤트로 시끄럽게
→ 장부 기록 + track_ops_applied / new_part_requested 이벤트
진입점: POST /debug/track-convo (오프너), GET /debug/tracks (관측)
```

---

## 예정된 변경 (순서대로 — 2026-08-18 피봇으로 재배열)

0. **리마인더 v0 → v1** — v0(2026-08-20, 약속이 행이 되고 15분
   cron이 send_nudge로 발화; 평일 반복은 LA 벽시계 기준 DST-안전)는
   운영자가 시딩한다. v1 = Theo가 답장 마커로 직접 등록
   (create_reminder가 이미 계약 지점 — 추출만 남음). 지휘 층(1번)이
   서면 리마인더 발화도 그 밑으로 들어간다.
1. **멀티트랙 지휘 층** — 트랙 여러 개가 하루 안에서 공존하는 법.
   터치포인트(아침/저녁)마다 표면화 후보 중 1-2개만 고른다 — 9개
   트랙이 각자 울리면 도움이 아니라 소음. 스펙은 머리로 쓰지 않고
   jeongmo 실사용의 실패 모양(뭐가 시끄럽고 뭐가 묻히는지)에서
   도출한다.
2. **캐싱 전제조건** — companion 프롬프트 구조를 처음부터 캐시
   친화적으로(안정 부분 앞, 시각·상태 뒤). 실측 근거: 드릴 하나가
   유저당 월 $10-18, 멀티트랙 헤비 유저는 무최적화 월 ~$90 —
   캐싱은 최적화가 아니라 성립 조건이다.
3. **비용 관측** — llm_calls에 토큰/비용 롤업 (관측성의 첫 조각,
   가격 결정이 실측 위에 서게)
4. **채굴 chunking 재설계** — 섹션 단위 채굴 + 컴플레인 루프 복귀
   (드릴 유지보수 — 남편이 쓰는 동안은 산다)
5. **가르쳐준 것 장부 소비자 연결** — 쓰기만 되고 아무도 안 읽는다
6. **Gmail 커넥터** — 트랙들이 서고 나서 (외부데이터 부품의 첫 구현)

**후순위로 내려간 것 (founder, 2026-08-18):** 오답노트 강화 —
"대부분의 유저는 남편과 같은 학습을 하지 않는다." 설계 논점들은
기록되어 있고(까먹음 vs 생략 구분, 코치가 유저의 실제 수행 방식을
안 물어봄, disambiguation 질문 채널) 드릴이 도는 동안 증거는 계속
쌓인다.
